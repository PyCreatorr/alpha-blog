<nav class="navbar navbar-dark bg-dark navbar navbar-expand-lg bg-body-tertiary ">
        <div class="container-fluid">
         
          <%= link_to "ALPHA BLOG", root_path, class: "navbar-brand", id: "logo" %>

          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <%= link_to 'Bloggers', users_path, class:"nav-link" %>
              </li>            
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Articles
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'View Articles',  articles_path, class:"dropdown-item" %></li>
                  
                  <% if logged_in? %>
                    <li><%= link_to 'Create Article',  new_article_path, class:"dropdown-item" %></li>
                  <% end %>
                  <!--# <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item" href="#">Something else here</a></li> -->
                </ul>
              </li>

              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                  Categories
                </a>
                <ul class="dropdown-menu">
                  <li><%= link_to 'View Categories', categories_path, class:"dropdown-item" %></li>
                  
                  <% if logged_in? && current_user.admin? %>
                    <li><%= link_to 'Create Categorie',  new_category_path, class:"dropdown-item" %></li>
                  <% end %>
                  <!--# <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item" href="#">Something else here</a></li> -->
                </ul>
              </li>

              <% if logged_in? %>
                  <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                      <%= '(Admin)' if current_user.admin? %> Profile [ <%= current_user.username%>]
                    </a>                    
                    <ul class="dropdown-menu">
                      <li><%= link_to 'View my profile', user_path(current_user), class:"dropdown-item" %></li>
                      <li><%= link_to 'Edit my profile',  edit_user_path(current_user), class:"dropdown-item" %></li>
                      <li><%= link_to 'Delete my profile',  user_path(current_user), data: {turbo_method: :delete, turbo_confirm: 'Are your sure?'}, class:"dropdown-item text-danger" %></li>
                  </ul>
                </li>

                  <li class="nav-item">
                    <%= link_to 'Log out', login_path, data: {turbo_method: :delete, turbo_confirm: 'Are your sure?'}, class:"nav-link" %>
                  </li>
                <% else %>
                    <li class="nav-item">
                      <%= link_to 'Login', login_path, class:"nav-link" %>
                  </li>
                    <li class="nav-item">
                      <%= link_to 'Sign up', signup_path, class:"nav-link" %>
                  </li>
                <% end %>
            </ul>
            <form class="d-flex" role="search">
              <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
              <button class="btn btn-outline-primary" type="submit">Search</button>
            </form>
          </div>
        </div>
      </nav>